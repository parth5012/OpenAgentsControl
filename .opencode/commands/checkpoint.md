---
description: "Checkpoint current branch: commit work, push to GitHub, open/refresh PR for later review (never auto-merge). Optional: commit message via $ARGUMENTS"
---

Invoke the `checkpoint` skill and follow its workflow exactly.

Commit message (if changes are uncommitted): $ARGUMENTS